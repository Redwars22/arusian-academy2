# 🛫 Words and Phrases for Travelling

import { FontRenderer } from "../src/components/FontRenderer"

export function RenderSemlek(props) {
  return <FontRenderer size="22px" weight="900" rtl color="var(--ifm-color-primary, #794cbd)" text={props.phrase} />
}

export function Audio(props){

    const getURL = (file) => {
        const BASE = "https://thearusianlanguage.vercel.app/audio/";

        return `${BASE}${file}`
    }

    return <>
            <div>
              <audio controls style={{width: '100px'}}>
                  <source src={getURL(props.file)} type="audio/mpeg"/>
              </audio>
            </div>
            <span>[{props.ipa}]</span>
        </>
}

|Arusian |Pronunciation|Translation|
|---|---|---|
| <RenderSemlek phrase={". . su s,u . ."}/> <br/> Suzu! | <Audio ipa="suzu" file="Suzu.mp3"/> | Hello/Goodbye (person A) |
| <RenderSemlek phrase={". . so so . ."}/> <br/> Soso! | <Audio ipa="soso" file="Soso.mp3"/> | Hello/Goodbye (person B) |
